# Python 3.10–3.12 recommended. Tested with the versions in the right column.
#
# IMPORTANT — PyTorch: the line below installs the default (often CPU) build.
# On a GPU machine, install a CUDA build FIRST, then `pip install -r requirements.txt`:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# (pick the cu1xx tag matching your CUDA). See https://pytorch.org/get-started/locally/
torch>=2.2                 # tested 2.11
numpy>=1.26,<3             # tested 2.2.6
scipy>=1.11               # tested 1.13.0
matplotlib>=3.7           # tested 3.8.4
mujoco>=3.1               # tested 3.7.0
gymnasium>=0.29           # tested 1.2.3
stable-baselines3>=2.3    # tested 2.8.0
scikit-learn>=1.3         # tested 1.4.2
optuna>=3.5               # tested 4.9.0
